public class Passenger extends User {

    public Passenger(String username,Location currentLocation, Location destination){
        super();
        setUsername(username);
        setCurrentLocation(currentLocation);
        setDestination(destination);
    }
}
